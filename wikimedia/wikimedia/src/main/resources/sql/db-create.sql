create database "wikimedia-data";

# If using IntelliJ, connect first to the db you want, by right-clicking -> new -> query console
# Then run the following script if wanting to create a new table
CREATE TABLE test (
       column1 INT,
       column2 VARCHAR(50)
);